<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Permite que la app pida permiso de micrófono -->
    <key>com.apple.security.device.audio-input</key>
    <true/>

    <!-- Permite que la app use Apple Events (para accesibilidad/typing/launch-at-login) -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Permite controlar System Events (necesario para launch at login) -->
    <key>com.apple.security.temporary-exception.apple-events</key>
    <array>
        <string>com.apple.systemevents</string>
    </array>

    <!-- Hardened runtime (requerido para notarización) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network (para API calls) -->
    <key>com.apple.security.network.client</key>
    <true/>
</dict>
</plist>
